# VS Code Python extension environment for test discovery/run.
# Force pytest wrapper to always run through mpiexec with --with-mpi.
monoprop_VSCODE_MPI_MODE=all
monoprop_MPI_TEST_PROCS=4
monoprop_MPI_ALLOW_RUN_AS_ROOT=1
