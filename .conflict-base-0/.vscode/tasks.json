{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Install in Release mode",
            "type": "shell",
            "command": "uv sync --group dev --group test --group interactive --all-extras -v",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Install in Debug mode",
            "type": "shell",
            "command": "uv sync --group dev --group test --group interactive --all-extras -v --config-settings=cmake.build-type='Debug'",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": false
            }
        }
    ]
}
