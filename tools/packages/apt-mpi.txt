# Added to tools/packages/apt.txt when an MPI runtime is requested. Kept apart so
# a serial job cannot acquire one implicitly: a build with MPI off must also be
# tested without a runtime present.

libopenmpi-dev          # MPI::MPI_CXX, and the headers mpi4py builds against
openmpi-bin             # mpiexec, for the multi-rank test and coverage runs
